// PWchange 다음 페이지 ; 비밀번호 변경 완료 페이지
import React from "react";

const PWResult = () => {
    return (
        <div>
            <h1>비밀번호 변경이 완료되었습니다.</h1>
        </div>
    );
    };

export default PWResult;
